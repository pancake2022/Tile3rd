using UnityEngine;
using CSFramework;
using System.Collections.Generic;

public class RewardData
{
    // public Config Config;
    // public int Count;

    // public Sprite CustomSprite;

    // public static List<RewardData> CreateList (List<Config> config_list, List<int> count_list)
    // {
    //     var list = new List<RewardData>();
    //     for (var i = 0; i < config_list.Count; ++i)
    //     {
    //         list.Add(new RewardData
    //         {
    //             Config = config_list[i],
    //             Count = i < count_list.Count ? count_list[i] : 1,
    //         });
    //     }
    //     return list;
    // }

    // public static List<RewardData> CreateList (List<int> config_id_list, List<int> count_list, List<Config> reward_config_list)
    // {
    //     var list = new List<RewardData>();
    //     for (var i = 0; i < config_id_list.Count; ++i)
    //     {
    //         var config_id = config_id_list[i];
    //         list.Add(new RewardData
    //         {
    //             Config = reward_config_list.Find(a => a.Id == config_id),
    //             Count = i < count_list.Count ? count_list[i] : 1,
    //         });
    //     }
    //     return list;
    // }
}